<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="{{asset('css/style.css')}}">
        <script src="https://kit.fontawesome.com/2bb2f2a2a8.js" crossorigin="anonymous"></script>

        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
    <header>
        <nav>
            <div class="left">
                <div class="logo"><a href="{{path('app_home')}}">Le Beau Coin</a></div>
                <div class="button new"><a href="{{path('user_annonces_ajout')}}">Déposer une annonce</a></div>
                
            </div>
            <div class="right">
                <div class="profil">
                    <div class="icone"><i class="fas fa-user"></i></div>
                    <div class="txt"><a href="{{path('user')}}">Profil</a></div>
                </div>

                <div class="logout">
                    <div class="icone"><i class="fas fa-sign-out-alt"></i></div>
                    <div class="txt"><a href="">Logout</a></div>
                </div>
               
            </div>
        </nav>
    </header>

    <main>
        {% block body %}{% endblock %}
    </main>

    <footer>
        <ul>
            <li><a href="">Copyright</a></li>    
            <li><a href="">Contact</a></li>    
        </ul>
    </footer>
    </body>
</html>
